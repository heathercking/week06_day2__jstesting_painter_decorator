const assert = require('assert')
const Paint = require('../paint.js');

describe('Paint', function() {
    let paint;

    beforeEach(function() {
        paint = new Paint('Yellow', 5);
    });

    it('should have a number of litres of paint')

    
    it('should be able to check if it is empty')


    it('should be able to empty itself of paint')


})